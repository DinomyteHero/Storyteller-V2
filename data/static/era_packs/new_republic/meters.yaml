# Meter definitions (v2) — bounds/defaults for persistent world meters.
meters:
  reputation_by_faction:
    min: -100
    max: 100
    default: 0
  heat_global:
    min: 0
    max: 100
    default: 0
    decay_per_tick: 1
  heat_by_location:
    min: 0
    max: 100
    default: 0
    decay_per_tick: 2
  control_shift:
    enabled: true
    cadence_ticks: 6
    max_delta_per_resolution: 12
    gate:
      heat_global_min: 20
      heat_by_location_min: 10
    notes: "Control changes are possible but should be the result of sustained pressure (raids, reforms, sabotage), not random churn."
