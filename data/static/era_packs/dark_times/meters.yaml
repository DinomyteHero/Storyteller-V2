# Meter definitions (v2) — bounds/defaults for persistent world meters.
meters:
  reputation_by_faction:
    min: -100
    max: 100
    default: 0
  heat_global:
    min: 0
    max: 100
    default: 0
    decay_per_tick: 1
  heat_by_location:
    min: 0
    max: 100
    default: 0
    decay_per_tick: 2
  control_shift:
    enabled: false

